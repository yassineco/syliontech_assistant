# =============================================================================
# CONFIGURATION MVP PERSISTANT - MAGIC BUTTON
# =============================================================================

# ===== MODE DE FONCTIONNEMENT =====
# false = Simulation (développement)
# true = Production (persistance réelle)
USE_REAL_EMBEDDINGS=false
USE_REAL_VECTOR_DB=false

# ===== CONFIGURATION GOOGLE CLOUD =====
# Projet GCP (requis pour production)
PROJECT_ID=sylion-tech-assistant

# Région pour Vertex AI et Firestore
VERTEX_AI_LOCATION=europe-west1
FIRESTORE_LOCATION=europe-west1

# Credentials GCP (optionnel si gcloud configuré)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ===== CONFIGURATION VERTEX AI =====
# Modèle d'embeddings (production)
VERTEX_AI_EMBEDDING_MODEL=text-embedding-004

# Batch size pour optimiser les coûts
VERTEX_AI_BATCH_SIZE=100

# ===== CONFIGURATION FIRESTORE =====
# Collection pour les vecteurs
FIRESTORE_COLLECTION=rag_vectors

# Index pour optimiser les recherches
FIRESTORE_INDEX_FIELD=vector

# ===== CONFIGURATION SIMULATION =====
# Dimension des vecteurs simulés
SIMULATED_VECTOR_DIMENSION=768

# Nombre max de documents en simulation
SIMULATED_MAX_DOCUMENTS=1000

# ===== CONFIGURATION GÉNÉRALE =====
# Port du serveur
PORT=8080

# Environnement
NODE_ENV=development

# Logs
LOG_LEVEL=info

# ===== COÛTS ET LIMITES =====
# Budget mensuel maximum (en USD)
MONTHLY_BUDGET_LIMIT=10.00

# Alertes de coût
COST_ALERT_THRESHOLD=5.00

# Limite quotidienne d'embeddings
DAILY_EMBEDDING_LIMIT=10000

# Limite quotidienne de recherches
DAILY_SEARCH_LIMIT=1000

# =============================================================================
# INSTRUCTIONS POUR LA DÉMO N+1
# =============================================================================
#
# 1. MODE DÉVELOPPEMENT (défaut) :
#    USE_REAL_EMBEDDINGS=false
#    USE_REAL_VECTOR_DB=false
#    → Coût : 0€ (simulation complète)
#
# 2. MODE HYBRIDE (démonstration) :
#    USE_REAL_EMBEDDINGS=true
#    USE_REAL_VECTOR_DB=false
#    → Coût : ~0.10€/mois (embeddings réels, stockage simulé)
#
# 3. MODE PRODUCTION (MVP réel) :
#    USE_REAL_EMBEDDINGS=true
#    USE_REAL_VECTOR_DB=true
#    → Coût : ~3.50€/mois (persistance complète)
#
# Pour basculer en temps réel :
# curl -X POST http://localhost:8080/demo/toggle-mode \
#   -H "Content-Type: application/json" \
#   -d '{"mode": "production"}'
#
# =============================================================================